
<form method="post" action="{{ path('event_delete', {'id': event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
    <a class="btn btn-primary" href="{{ path('event_index') }}">retour à la liste</a>
    <a class="btn btn-secondary" href="{{ path('event_edit', {'id': event.id, 'fromCalendar' : 0}) }}">modifier</a>
    <button class="btn btn-danger">Delete</button>
</form>
